<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
  공통코드 조회. 값 검증에 쓴다.
  공통코드 관리 기능이 만들어지면 그쪽 매퍼로 옮긴다.
-->
<mapper namespace="com.fulfillment.common.code.dao.CodeDao">

    <select id="selectCodeIds" resultType="string">
        SELECT c.code_id
          FROM tb_code c
          JOIN tb_code_group g ON g.code_group_seq = c.code_group_seq
         WHERE g.code_group_id = #{groupId}
           AND g.use_yn = 'Y'
           AND c.use_yn = 'Y'
         ORDER BY c.sort_order
    </select>

</mapper>
